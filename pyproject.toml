[project]
name = "evlog-tools"
version = "0.1.0"
description = "Analysis and report scripts for evlog experiments"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9",
]

[tool.uv]
package = false
