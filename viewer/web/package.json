{
  "name": "evlog-viewer-web",
  "private": true,
  "type": "module",
  "description": "The viewer's front end. The sources are src/*.ts; tsc writes js/*.js, which is what the browser gets and what is committed, so watching a world needs nothing installed.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "three": "0.169.0",
    "typescript": "^5.9.0"
  }
}
