//! e060 runs no world: it is an analysis of existing runs (#71). The workspace takes every
//! experiment folder as a crate, so this stub is here. See `census.py` and `README.md`.

fn main() {
    println!("e060 is an analysis only: uv run python experiments/e060_census/census.py");
}
