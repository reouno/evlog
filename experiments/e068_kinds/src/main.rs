//! e068 runs no world: it is an analysis of e065-e067's censuses (#82). The workspace takes every
//! experiment folder as a crate, so this stub is here. See `kinds.py` and `README.md`.

fn main() {
    println!("e068 is an analysis only: uv run python experiments/e068_kinds/kinds.py");
}
