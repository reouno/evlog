//! e083 runs no world: it is an analysis of e081's and e082's censuses (#96). The workspace takes every
//! experiment folder as a crate, so this stub is here. See `hold.py` and `README.md`.

fn main() {
    println!("e083 is an analysis only: uv run python experiments/e083_hold/hold.py");
}
